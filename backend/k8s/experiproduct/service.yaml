apiVersion: v1
kind: Service
metadata:
  name: experiproduct
  namespace: backend
spec:
  selector:
    app: experiproduct
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: LoadBalancer
